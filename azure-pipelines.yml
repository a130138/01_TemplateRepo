# File Transformation POC

trigger:
- none

pool:
  vmImage: windows-latest

variables:
  resources.tags.Env:  'testing'

steps:
# - script: dir /s

- task: FileTransform@1
  inputs:
    folderPath: 'D:\a\1\s\'
    targetFiles: 'azuredeploy.json'
    fileType: json
