# File Transformation POC

trigger:
- none

pool:
  vmImage: windows-latest

variables:
  resources.tags.Owner:  'joecro'

steps:
- task: FileTransform@1
  inputs:
    folderPath: '$(System.DefaultWorkingDirectory)/azuredeploy.json'
    targetFiles: '/azuredeploy.json'
    fileType: json