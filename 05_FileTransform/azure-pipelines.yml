# File Transformation POC

trigger:
- none

pool:
  vmImage: windows-latest

variables:
# - group: JsonVariableGrp
  - name: owner
    value:  'jc'
  - name: env
    value: 'POC'

steps:
- task: PowerShell@2
  inputs:
      targetType: inline
      script: |
        (Get-Content -Path "$(System.DefaultWorkingDirectory)\05_FileTransform\azuredeploy.json") | foreach($_.replace($owner,$env))

#- task: FileTransform@1
#  displayName: "File Transform Task"
#  inputs:
#    folderPath:  '$(System.DefaultWorkingDirectory)/05_FileTransform/'
#    targetFiles: 'azuredeploy.json'
#    fileType: 'json'